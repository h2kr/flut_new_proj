import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

Container processesScreen() {
  return Container(
      child: Center(
        child: Text(
          "PROCESSES",
          style: TextStyle(color: Colors.redAccent),
        ),
      ));
}